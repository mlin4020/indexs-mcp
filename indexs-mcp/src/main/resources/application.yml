server:
  port: 3000

spring:
  application:
    name: indexs-mcp

mcp:
  server:
    api-base-url: https://api.example.com/metrics
    timeout: 30
    headers:
      Content-Type: application/json

logging:
  level:
    com.indexs.mcp: INFO
    org.springframework.web: INFO
