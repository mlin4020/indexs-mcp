version: '3.8'
services:
  indexs-mcp:
    build: .
    container_name: indexs-mcp
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./config.json:/app/config.json:ro
      - ./tools:/app/tools:ro
