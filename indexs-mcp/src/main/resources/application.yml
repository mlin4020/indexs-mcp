server:
  port: 3000

spring:
  application:
    name: indexs-mcp
  ai:
    mcp:
      server:
        enabled: true
        base-path: /mcp

mcp:
  server:
    api-base-url: https://api.example.com/metrics

logging:
  level:
    com.indexs.mcp: INFO
    org.springframework.web: INFO
    org.springframework.ai: INFO
